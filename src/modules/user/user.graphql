directive @role(requires: Role = ADMIN) on OBJECT | FIELD_DEFINITION

type User {
  id: String!
  fullName: String!
  email: String!
  role: Role!
  password: String! @role(requires: ADMIN)
}

extend type Query {
  me: User
}
